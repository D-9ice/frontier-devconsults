'use client';

import { Bot, RotateCcw, Send, X } from 'lucide-react';
import { FormEvent, useEffect, useRef, useState } from 'react';

type Message = { role: 'user' | 'assistant'; content: string };
export default function AssistantWidget() {
  const [open, setOpen] = useState(false); const [messages, setMessages] = useState<Message[]>([]); const [input, setInput] = useState(''); const [pending, setPending] = useState(false); const [error, setError] = useState('');
  const launcher = useRef<HTMLButtonElement>(null); const close = useRef<HTMLButtonElement>(null);
  useEffect(() => { const dismiss = () => setOpen(false); window.addEventListener('frontier-whatsapp-open', dismiss); return () => window.removeEventListener('frontier-whatsapp-open', dismiss); }, []);
  useEffect(() => { if (open) close.current?.focus(); }, [open]);
  const toggle = () => { const next = !open; setOpen(next); if (next) window.dispatchEvent(new Event('frontier-assistant-open')); };
  const dismiss = () => { setOpen(false); requestAnimationFrame(() => launcher.current?.focus()); };
  const submit = async (event: FormEvent) => { event.preventDefault(); const content = input.trim(); if (!content || pending) return; const userMessage: Message = { role: 'user', content }; const next = [...messages, userMessage].slice(-8); setMessages(next); setInput(''); setError(''); setPending(true); try { const response = await fetch('/api/assistant', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ messages: next }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'The assistant is unavailable.'); const answer: Message = { role: 'assistant', content: String(data.answer) }; setMessages([...next, answer].slice(-8)); } catch (cause) { setError(cause instanceof Error ? cause.message : 'The assistant is unavailable.'); } finally { setPending(false); } };
  return <>{open && <section role="dialog" aria-modal="false" aria-labelledby="assistant-title" onKeyDown={(event) => { if (event.key === 'Escape') dismiss(); }} className="fixed bottom-24 right-3 z-[60] flex max-h-[min(38rem,calc(100vh-7rem))] w-[calc(100vw-1.5rem)] max-w-sm flex-col overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-2xl sm:right-6"><header className="flex items-center justify-between bg-gradient-to-r from-blue-700 to-indigo-700 p-4 text-white"><div className="flex items-center gap-3"><Bot className="h-6 w-6" /><div><h2 id="assistant-title" className="font-bold">Frontier Assistant</h2><p className="text-xs text-blue-100">AI-generated website guidance</p></div></div><button ref={close} type="button" onClick={dismiss} aria-label="Close Frontier Assistant" className="rounded-lg p-2 hover:bg-white/15 focus:outline-none focus:ring-2 focus:ring-white"><X className="h-5 w-5" /></button></header><div className="flex-1 space-y-3 overflow-y-auto bg-gray-50 p-4" aria-live="polite">{messages.length === 0 && <div className="rounded-xl bg-white p-4 text-sm leading-6 text-gray-700 shadow-sm"><p className="font-semibold text-gray-900">How can I help?</p><p className="mt-1">Ask about our published services, apps, solutions, or engagement options.</p></div>}{messages.map((message, index) => <div key={`${message.role}-${index}`} className={`max-w-[90%] rounded-xl p-3 text-sm leading-6 ${message.role === 'user' ? 'ml-auto bg-blue-600 text-white' : 'bg-white text-gray-800 shadow-sm'}`}>{message.content}</div>)}{pending && <p className="text-sm text-gray-600">Frontier Assistant is responding…</p>}{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}</div><form onSubmit={submit} className="border-t border-gray-200 bg-white p-3"><label htmlFor="assistant-message" className="sr-only">Message Frontier Assistant</label><div className="flex gap-2"><input id="assistant-message" value={input} onChange={(event) => setInput(event.target.value)} maxLength={2000} placeholder="Ask about our services…" className="min-w-0 flex-1 rounded-lg border border-gray-300 px-3 py-2" /><button type="submit" disabled={pending || !input.trim()} aria-label="Send message" className="rounded-lg bg-blue-600 p-2.5 text-white hover:bg-blue-700 disabled:opacity-50"><Send className="h-5 w-5" /></button><button type="button" onClick={() => { setMessages([]); setError(''); }} aria-label="Clear conversation" className="rounded-lg border border-gray-300 p-2.5 text-gray-700 hover:bg-gray-50"><RotateCcw className="h-5 w-5" /></button></div><p className="mt-2 text-xs text-gray-500">Do not share passwords, payment details, health records, or confidential information.</p></form></section>}<button ref={launcher} type="button" onClick={toggle} aria-expanded={open} aria-controls="assistant-title" className="fixed bottom-6 right-6 z-50 flex h-14 w-14 items-center justify-center rounded-full bg-blue-700 text-white shadow-lg hover:bg-blue-800 focus:outline-none focus:ring-4 focus:ring-blue-200" aria-label={open ? 'Close Frontier Assistant' : 'Open Frontier Assistant'}>{open ? <X className="h-6 w-6" /> : <Bot className="h-7 w-7" />}</button></>;
}
