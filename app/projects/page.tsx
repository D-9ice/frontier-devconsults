import type { Metadata } from 'next';
import Link from 'next/link';
import { BriefcaseBusiness } from 'lucide-react';
import { listApps } from '@/lib/apps';
import { canonicalAppName, compactSummary, lifecycleLabel, solutionKindLabel } from '@/lib/application-presentation';
import { listProjects } from '@/lib/projects';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Projects & Case Studies', description: 'Evidence-based software and engineering work published by Frontier DevConsults.', alternates: { canonical: '/projects' } };

export default async function ProjectsPage() {
  let projects: Awaited<ReturnType<typeof listProjects>> = []; let apps: Awaited<ReturnType<typeof listApps>> = [];
  try { [projects, apps] = await Promise.all([listProjects(false), listApps(false)]); } catch (error) { console.error('Public projects fetch failed:', error); }
  const appProjects = apps.filter((app) => app.showInProjects && app.slug);
  return <main className="min-h-screen bg-gray-50"><section className="bg-gradient-to-br from-slate-950 to-blue-900 py-20 text-white"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><p className="font-bold uppercase tracking-[0.18em] text-blue-300">Selected work</p><h1 className="mt-4 text-4xl font-bold sm:text-5xl">Projects &amp; Case Studies</h1><p className="mt-5 max-w-3xl text-xl leading-8 text-slate-200">Published work presented with its real lifecycle, technical scope, and owner-approved evidence.</p></div></section><section className="py-14"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">{projects.length === 0 && appProjects.length === 0 ? <div className="rounded-2xl border border-dashed border-gray-300 bg-white p-12 text-center"><BriefcaseBusiness className="mx-auto h-10 w-10 text-gray-400" /><h2 className="mt-4 text-2xl font-bold text-gray-900">No published case studies yet</h2><p className="mt-2 text-gray-600">Approved project evidence will appear here after publication.</p></div> : <div className="grid gap-6 md:grid-cols-2">{appProjects.map((app) => <article key={app.id} className="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><div className="flex gap-4">{app.iconUrl && <img src={app.iconUrl} alt="" className="h-14 w-14 rounded-lg object-contain" />}<div><h2 className="text-xl font-bold text-gray-900">{canonicalAppName(app.name)}</h2><p className="mt-1 text-sm font-semibold text-blue-700">{solutionKindLabel(app.solutionKind)} · {lifecycleLabel(app.status)}</p></div></div><p className="mt-5 leading-7 text-gray-700">{compactSummary(app.solutionSummary || app.description, 300)}</p><div className="mt-4 flex flex-wrap gap-2">{app.technologies.slice(0, 5).map((item) => <span key={item} className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-semibold text-gray-700">{item}</span>)}</div><Link href={`/projects/${encodeURIComponent(app.slug!)}`} className="mt-6 inline-flex font-bold text-blue-700 hover:text-blue-900">View case study →</Link></article>)}{projects.map((project) => <article key={project.id} className="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><div className="flex gap-4">{project.logoUrl && <img src={project.logoUrl} alt="" className="h-14 w-14 rounded-lg object-contain" />}<div><h2 className="text-xl font-bold text-gray-900">{project.title}</h2><p className="mt-1 text-sm font-semibold text-blue-700">{project.category} · {project.status}</p></div></div><p className="mt-5 leading-7 text-gray-700">{compactSummary(project.description, 300)}</p><div className="mt-4 flex flex-wrap gap-2">{project.technologies.slice(0, 5).map((item) => <span key={item} className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-semibold text-gray-700">{item}</span>)}</div>{project.liveLink && <a href={project.liveLink} target="_blank" rel="noopener noreferrer" className="mt-6 inline-flex font-bold text-blue-700 hover:text-blue-900">Visit published project →</a>}</article>)}</div>}</div></section></main>;
}
